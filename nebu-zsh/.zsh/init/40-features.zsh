# Load user modules.

[[ -r "$HOME/.zsh/modern-commands.zsh" ]] && source "$HOME/.zsh/modern-commands.zsh"
[[ -r "$HOME/.zsh/aliases.zsh" ]] && source "$HOME/.zsh/aliases.zsh"
[[ -r "$HOME/.zsh/snapper.zsh" ]] && source "$HOME/.zsh/snapper.zsh"
[[ -r "$HOME/.zsh/nvim.zsh" ]] && source "$HOME/.zsh/nvim.zsh"

[[ -r "$HOME/.zsh/custom/cleantmp.zsh" ]] && source "$HOME/.zsh/custom/cleantmp.zsh"
[[ -r "$HOME/.zsh/custom/detach.zsh" ]] && source "$HOME/.zsh/custom/detach.zsh"
[[ -r "$HOME/.zsh/custom/tmux.zsh" ]] && source "$HOME/.zsh/custom/tmux.zsh"

[[ -r "$HOME/.zsh/fzf/fzf-movie.zsh" ]] && source "$HOME/.zsh/fzf/fzf-movie.zsh"
[[ -r "$HOME/.zsh/fzf/fzf-aliases.zsh" ]] && source "$HOME/.zsh/fzf/fzf-aliases.zsh"
[[ -r "$HOME/.zsh/fzf/fzf-files.zsh" ]] && source "$HOME/.zsh/fzf/fzf-files.zsh"

[[ -r "$HOME/.zsh/custom/yazi.zsh" ]] && source "$HOME/.zsh/custom/yazi.zsh"
